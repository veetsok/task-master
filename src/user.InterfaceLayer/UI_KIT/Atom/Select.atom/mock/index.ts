import SelectEnum from "../enum";

const SelectsMock = {
  children: "Демо",
  type: SelectEnum.enum_defaultSelect,
};

export default SelectsMock;

export const ISelectPropsDefault = {
  children: SelectsMock.children,
  type: SelectsMock.type,
};
